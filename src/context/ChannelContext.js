// Libraries

import { createContext } from "react";

// Functions

/**
 * It creates a context that allows to save locally all the channels before to be send it to the server
 */

export const ChannelContext = createContext();
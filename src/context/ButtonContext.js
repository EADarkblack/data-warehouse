// Libraries

import { createContext } from "react";

// Functions

/**
 * This context was created to save the state of the "edit location" button on the companies' modal.
 */

export const ButtonContext = createContext();
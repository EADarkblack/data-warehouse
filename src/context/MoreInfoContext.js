// Libraries

import { createContext } from "react";

// Functions

/**
 * This context was created to created the "more info section" on the companies' modal.
 */

export const MoreInfoContext = createContext("");
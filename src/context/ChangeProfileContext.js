// Libraries

import { createContext } from "react";

// Functions

/**
 * It creates a context that allows to handle the user type when you log in the app
 */

export const ChangeProfileContext = createContext();
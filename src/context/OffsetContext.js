// Libraries

import { createContext } from "react";

// Functions

/**
 * Creates a context to handle the position on the data base that can be render on the app.
 */

export const OffsetContext = createContext("");
// Libraries

import { createContext } from "react";

// Functions

/**
 * It creates a context that allows to keep the query in the sorting events in datatable
 */

export const QueryContext = createContext("");